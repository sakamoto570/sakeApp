{
  "name": "sake-recommend-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "infra",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@sake-app/frontend",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  }
}

